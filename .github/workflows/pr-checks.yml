# Pull request quality checks and preview deployment.
#
# Triggered on PRs targeting `main` or any `base/**` branch (epic-PRs in
# the zfb migration parity super-epic stack into base/zfb-migration-parity
# so each child epic-PR also needs CI gating).
#
# CI jobs (in approximate execution order):
#   Template Drift Check     — pure-shell, no install; scripts/check-template-drift.sh
#   No-Host-Alias Guard      — pure-Node, no install; scripts/check-no-host-alias-in-package.mjs
#   Pin Parity Check         — pure-Node, no install; scripts/check-pin-parity.mjs
#   Fixture Settings Drift   — pure-Node, no install; scripts/check-fixture-settings-drift.mjs
#   Package Safelist Check   — pnpm install + package build; scripts/check-package-safelist.mjs
#   B4push/CI Parity Check   — pure-Node, no install; scripts/check-b4push-ci-parity.mjs
#   E2E Spec Naming Guard    — pure-Node, no install; scripts/check-e2e-spec-naming.mjs
#   Flaky Tracking-Issue Guard — pure-Node, no install; scripts/check-flaky-tracking-issue.mjs
#   Wait-Debt Guard           — pure-Node, no install; scripts/check-wait-debt.mjs
#   Lint Gates               — pnpm install; tag guards + pnpm lint:tokens
#   Type Check               — pnpm install; pnpm check (zfb check → tsc --noEmit)
#   Worker Contract Proof    — Worker typecheck + runtime DO tests + Wrangler dry-run
#   Package Unit Tests       — pnpm install; pnpm test:packages (all 5 package suites)
#   Root Unit Tests          — pnpm install; pnpm test:unit (src/**/__tests__, scripts/__tests__)
#   Build Site               — full clone, pnpm install; pnpm build + check:links
#   HTML validate            — needs build-site; pnpm check:html
#   Build Doc History        — full clone, pnpm install; doc-history-server generate
#   E2E Tests                — pnpm install (Playwright container); pnpm test:e2e:ci
#   Preview Deploy           — needs build-site + build-history; wrangler deploy + smoke gates
#
# zfb and zdtp are consumed directly from npm (see package.json):
#   - @takazudo/zfb + @takazudo/zfb-runtime + @takazudo/zfb-adapter-cloudflare
#     ship the CLI; the actual prebuilt Rust binary arrives via a
#     platform-specific optionalDependency (@takazudo/zfb-<platform>) that
#     `pnpm install` resolves automatically. On the ubuntu-latest runners and
#     the Playwright container (both linux-x64) that is @takazudo/zfb-linux-x64-gnu.
#   - @takazudo/zdtp ships its own prebuilt dist/.
# There is no from-source clone/build/link step anymore — a plain
# `pnpm install --frozen-lockfile` is all each job needs. Dep versions are
# pinned in package.json (single source of truth).
#
# Inter-job data sharing uses upload-artifact/download-artifact (retention-days: 1
# keeps storage bounded; artifacts are the purpose-built mechanism for intra-run handoff).
#
# Concurrency: cancel in-progress runs for the same PR to save resources.

name: PR Checks

on:
  pull_request:
    branches:
      - main
      - 'base/**'

concurrency:
  group: pr-checks-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  # ---------------------------------------------------------------------------
  # Job 0: Template drift check (lightweight, no pnpm install needed)
  # ---------------------------------------------------------------------------
  check-template-drift:
    name: Template Drift Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check template drift
        run: bash scripts/check-template-drift.sh

  # ---------------------------------------------------------------------------
  # Job 0.4: No-host-alias-in-package guard (#2344, S1a)
  # ---------------------------------------------------------------------------
  # Fails if any source file under packages/zudo-doc/src/** imports the host
  # `@/` path alias, which does not exist in published/scaffolded consumers —
  # a silent package-boundary leak. Structural enforcement of the package-first
  # core constraint.
  # Pure-Node script — uses preinstalled Node on the runner image, no pnpm.
  check-no-host-alias-in-package:
    name: No-Host-Alias-In-Package Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check no host alias in package
        run: node scripts/check-no-host-alias-in-package.mjs

  # ---------------------------------------------------------------------------
  # Job 0.5: Pin parity check (lightweight, no pnpm install needed)
  # ---------------------------------------------------------------------------
  # Verifies @takazudo/zfb / @takazudo/zfb-runtime pins are identical
  # between root package.json and packages/create-zudo-doc/src/scaffold.ts.
  # Failure mode "W1A §5#4 — three pin sources, only one bumped" (W4A — #1732).
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-pin-parity:
    name: Pin Parity Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check pin parity
        run: node scripts/check-pin-parity.mjs

  # ---------------------------------------------------------------------------
  # Job 0.6: Fixture settings drift check (lightweight, no pnpm install needed)
  # ---------------------------------------------------------------------------
  # Verifies that every top-level key in src/config/settings.ts (canonical)
  # is either present in each of the five e2e fixture settings.ts files or
  # explicitly listed in .fixture-settings-drift-allowlist.
  # Closes the heading-ID parity gap (#1946): that drift reached main because
  # this check existed only in pnpm b4push, not CI.
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-fixture-settings-drift:
    name: Fixture Settings Drift Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check fixture settings drift
        run: node scripts/check-fixture-settings-drift.mjs

  # ---------------------------------------------------------------------------
  # Job 0.63: Package safelist drift check (#1982, updated #1994)
  # ---------------------------------------------------------------------------
  # Verifies that the generated dist/safelist.css in packages/zudo-doc/
  # covers every responsive-variant + arbitrary-value utility class used in
  # packages/zudo-doc/src/**/*.tsx. Closes the silent-drift gap from #1971:
  # a new bracket/responsive utility in the package must be captured by
  # gen-safelist.mjs; this check catches it if it is not.
  #
  # Requires pnpm install + package build: the guard reads dist/safelist.css,
  # which is generated by `pnpm --filter @takazudo/zudo-doc build` (#1994).
  check-package-safelist:
    name: Package Safelist Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @takazudo/zudo-doc
        run: pnpm --filter @takazudo/zudo-doc build

      - name: Check package safelist drift
        run: node scripts/check-package-safelist.mjs

  # ---------------------------------------------------------------------------
  # Job 0.65: B4push/CI parity check (guard manifest meta-check)
  # ---------------------------------------------------------------------------
  # Verifies that every lightweight guard gate in scripts/run-b4push.sh also
  # has a corresponding job in this workflow. Closes the silent-drift gap where
  # a b4push guard could be added without a matching CI job (#1967).
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-b4push-ci-parity:
    name: B4push/CI Parity Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check b4push/CI parity
        run: node scripts/check-b4push-ci-parity.mjs

  # ---------------------------------------------------------------------------
  # Job 0.67: E2E spec naming guard (#2095)
  # ---------------------------------------------------------------------------
  # Asserts (a) every e2e/*.spec.ts filename starts with a known fixture prefix
  # so Playwright's testMatch glob actually picks it up, and (b) no *.spec.ts
  # files exist outside e2e/ except those listed in .check-e2e-spec-naming-allowlist.
  # A misnamed spec silently matches no project and never runs — this guard
  # catches that silent omission.
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-e2e-spec-naming:
    name: E2E Spec Naming Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check E2E spec naming
        run: node scripts/check-e2e-spec-naming.mjs

  # ---------------------------------------------------------------------------
  # Job 0.69: @flaky/@local-only tracking-issue guard (#2292)
  # ---------------------------------------------------------------------------
  # Asserts every test tagged @flaky or @local-only in e2e/**/*.spec.ts has a
  # GitHub issue URL in a comment on the line(s) immediately preceding the
  # test() call. Without the URL, quarantine telemetry (report-flaky-lane.mjs)
  # is silent and the exit deadline is untracked — the exact graveyard pattern
  # the @flaky refactor was designed to prevent.
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-flaky-tracking-issue:
    name: Flaky Tracking-Issue Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check @flaky/@local-only tracking-issue URLs
        run: node scripts/check-flaky-tracking-issue.mjs

  # ---------------------------------------------------------------------------
  # Job 0.695: Wait-debt guard (#2538)
  # ---------------------------------------------------------------------------
  # Zero-tolerance enforcement of TESTING.md's Wait-Pattern Rules: every
  # `waitForTimeout` call in e2e/ (excluding fixtures) must carry a trailing
  # `// wait-ok: <why>` comment on the same line. Closes the enforcement gap
  # where the rule was documented but nothing mechanically checked for it —
  # the same failure mode the @flaky tracking-issue guard above closed for
  # quarantine telemetry.
  # Pure-Node script — uses preinstalled Node 22 on the runner image, no pnpm.
  check-wait-debt:
    name: Wait-Debt Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check wait-debt (waitForTimeout wait-ok annotations)
        run: node scripts/check-wait-debt.mjs

  # ---------------------------------------------------------------------------
  # Job 0.69: Component-tokens codegen drift check (#2448)
  # ---------------------------------------------------------------------------
  # Re-runs the --zdc-* component token codegen into a buffer and fails if
  # packages/zudo-doc/src/content.css has drifted from
  # packages/zudo-doc/src/config/component-tokens.ts (the single source of truth).
  # Pure-Node bin — uses preinstalled Node 22 on the runner image, no pnpm.
  # The generator ships as the @takazudo/zudo-doc `gen-component-tokens` bin
  # (#2448); CI invokes the in-repo bin file directly (dependency-free ESM).
  check-component-tokens:
    name: Component-Tokens Codegen Drift Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Check component-tokens codegen drift
        run: node packages/zudo-doc/bin/gen-component-tokens.mjs --check

  # ---------------------------------------------------------------------------
  # Job 0.7: Lint gates (tag guards + design token lint)
  # ---------------------------------------------------------------------------
  # Runs the vocabulary audit, the canonical authored/template data guard, and
  # pnpm lint:tokens (design-token-lint from @takazudo/zudo-design-token-lint).
  # The installed tag-audit and lint bins require
  # the installed node_modules, so a pnpm install is needed. Combined into one
  # job (single install, two run steps) to avoid a redundant install.
  lint-gates:
    name: Lint Gates
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tags audit
        run: pnpm tags:audit --ci

      - name: Check canonical tag data
        run: pnpm check:canonical-tags

      - name: Check current-only compatibility contract
        run: pnpm check:compatibility-contract

      - name: Run design token lint
        run: pnpm lint:tokens

  # ---------------------------------------------------------------------------
  # Job 1: Type checking
  # ---------------------------------------------------------------------------
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run type checking
        run: pnpm check

      # pages/ typecheck: root tsconfig excludes pages/ by design (zfb injects
      # its own page-runner types at build time). tsconfig.pages.json provides
      # the needed zfb/* path mappings so the ~50 page files that had zero
      # typecheck coverage are now gated in CI (#2018).
      - name: Run pages/ type checking
        run: pnpm check:pages

      # Workspace package typechecks: `pnpm check` (root tsconfig) excludes
      # packages/ so a broken package typecheck was previously invisible in CI.
      # Mirrors the per-package typecheck step that local b4push already runs.
      - name: Run workspace package typechecks
        run: pnpm -r --filter './packages/*' typecheck

  # ---------------------------------------------------------------------------
  # Job 1.25: Custom Worker + Durable Object contract
  # ---------------------------------------------------------------------------
  # Build-before-Wrangler is mandatory: worker-entry.ts imports the adapter's
  # dist/_worker.js, which in turn retains dist/_zfb_inner.mjs.
  worker-contract:
    name: Worker Contract Proof
    needs: build-site
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}
      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Download exact site build output
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: site-dist
          path: ${{ runner.temp }}
      - name: Extract site build output
        run: mkdir -p dist && tar -xf "$RUNNER_TEMP/site-dist.tar" -C dist
      - name: Type-check custom Worker and bindings
        run: pnpm check:worker
      - name: Run Workers-runtime Durable Object tests against built output
        run: pnpm test:worker:built
      - name: Verify Wrangler production dry-run against the same output
        run: node scripts/verify-worker-dry-run.mjs --skip-build

  # ---------------------------------------------------------------------------
  # Job 1.5: Package unit suites
  # ---------------------------------------------------------------------------
  # Guards the gap from #1851: package-scoped vitest suites previously ran
  # only inside publish workflows, so a broken suite was invisible until a
  # publish attempt. This job runs all 5 package suites on every PR so a
  # broken suite turns the PR red instead of hiding until release day.
  #
  # Build step precedes the test run for clean-runner parity: @takazudo/zudo-doc
  # ships compiled dist/ (built by tsup). The publish workflow runs
  # `pnpm --filter @takazudo/zudo-doc build` before its test step for the same
  # reason — a fresh CI runner has no pre-existing dist/ and the suite may
  # import it.
  package-tests:
    name: Package Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build @takazudo/zudo-doc before testing — clean-runner parity; the
      # suite may import built dist/ which does not exist on a fresh runner.
      - name: Build @takazudo/zudo-doc
        run: pnpm --filter @takazudo/zudo-doc build

      - name: Run package unit tests
        run: pnpm test:packages

      - name: Check current and retired package subpath resolution
        run: pnpm --filter @takazudo/zudo-doc test:plugin-resolution

  # ---------------------------------------------------------------------------
  # Job 1.6: Root unit suite
  # ---------------------------------------------------------------------------
  # Guards the gap from #1856: the root `test:unit` suite (vitest run, backed
  # by the root vitest.config.ts — src/**/__tests__ and scripts/__tests__) ran
  # in no CI workflow and was not in `pnpm b4push`, so a broken root-src/scripts
  # test could land on a PR (and main) without turning anything red. This is a
  # distinct scope from package-tests (#1851), which only gates packages/*.
  #
  # Kept as a separate named check (not folded into Package Unit Tests) so the
  # `Package Unit Tests` context name stays stable for branch-protection /
  # ruleset required-checks (#1857).
  #
  # Build step precedes the test run for clean-runner parity: the suite imports
  # @takazudo/zudo-doc/theme, whose compiled dist/ (built by tsup) does not
  # exist on a fresh runner — same reason package-tests builds it first.
  root-tests:
    name: Root Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build @takazudo/zudo-doc before testing — clean-runner parity; the root
      # suite imports built dist/ (@takazudo/zudo-doc/theme) which does not
      # exist on a fresh runner.
      - name: Build @takazudo/zudo-doc
        run: pnpm --filter @takazudo/zudo-doc build

      - name: Run root unit tests
        run: pnpm test:unit

  # ---------------------------------------------------------------------------
  # Job 2: Build the zfb docs site (full clone — needed for SSG meta dates)
  # ---------------------------------------------------------------------------
  # fetch-depth: 0 is required so the doc-history preBuild step can call
  # `git log --follow` on each content file and populate
  # `.zfb/doc-history-meta.json` with real author/date values. Those
  # values are statically inlined by esbuild into the SSG HTML at build
  # time (via the #doc-history-meta tsconfig alias). A shallow clone
  # produces an empty manifest, which causes DocMetainfoArea to return
  # null and the visible Created/Updated/Author block to be absent from
  # every deployed page (#1479, SSG meta gap).
  #
  # SKIP_DOC_HISTORY is intentionally NOT set here: we want the preBuild
  # runner to execute with full git access. The build-history job still
  # runs in parallel to generate the per-page dropdown JSON files for the
  # DocHistory island.
  build-site:
    name: Build Site
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: pnpm build

      - name: Check links
        # Strict mode to match scripts/run-b4push.sh — bare check:links is
        # non-strict (exit 0 on findings), which previously false-greened real
        # broken links on main (zudolab/zudo-doc#1985).
        run: pnpm run check:links -- --strict-broken --strict-absolute --allowlist=.check-links-allowlist

      - name: Archive site build output
        # tar preserves arbitrary route paths (upload-artifact rejects ':' in
        # paths, e.g. dist/docs/tags/type:guide/) and includes dotfiles.
        run: tar -cf "$RUNNER_TEMP/site-dist.tar" -C dist .

      - name: Upload site build output
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: site-dist
          path: ${{ runner.temp }}/site-dist.tar
          retention-days: 1

  # ---------------------------------------------------------------------------
  # Job: HTML validate (block-in-inline guard for #1490 regression)
  # ---------------------------------------------------------------------------
  # Runs html-validate against the full dist/ produced by build-site. Catches
  # element-permitted-content violations (e.g. <span><pre>) before merge.
  html-validate:
    name: HTML validate
    needs: [build-site]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install deps (no scripts)
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Download site build output (from build-site)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: site-dist
          path: ${{ runner.temp }}
      - name: Extract site build output
        run: mkdir -p dist && tar -xf "$RUNNER_TEMP/site-dist.tar" -C dist

      - name: Run html-validate
        run: pnpm check:html

  # ---------------------------------------------------------------------------
  # Job 3: Build doc history JSONs (full clone — only this job needs it)
  # ---------------------------------------------------------------------------
  # The @takazudo/zudo-doc-history-server package is fully standalone (only depends
  # on @types/node + tsup + tsx + typescript + vitest — no zfb consumption).
  # fetch-depth: 0 so it can walk git history for every content file.
  build-history:
    name: Build Doc History
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate doc history
        run: |
          pnpm --filter @takazudo/zudo-doc-history-server generate \
            --content-dir src/content/docs \
            --locale ja:src/content/docs-ja \
            --out-dir ../../doc-history-out

      - name: Archive doc history output
        run: tar -cf "$RUNNER_TEMP/doc-history.tar" -C doc-history-out .

      - name: Upload doc history output
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: doc-history
          path: ${{ runner.temp }}/doc-history.tar
          retention-days: 1

  # ---------------------------------------------------------------------------
  # Job 4: E2E & smoke tests
  # ---------------------------------------------------------------------------
  # The Playwright container ships Node + browsers; the linux-x64 zfb binary
  # arrives via the platform optionalDependency at `pnpm install` time.
  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Job-level override: the workflow-level permissions (top) grant only
    # contents: read + pull-requests: write. This job additionally needs
    # issues: write so "Report retry flakes" can file/append a retry-flake
    # issue (zudolab/zudo-doc#2535). Fork PRs still get a read-only token
    # regardless of this grant (GitHub platform rule) — the script degrades
    # to annotation-only in that case; see scripts/report-retry-flakes.mjs.
    permissions:
      contents: read
      issues: write
    container:
      # renovate: datasource=docker depName=mcr.microsoft.com/playwright versioning=docker
      image: mcr.microsoft.com/playwright:v1.58.2-noble
      options: --user 1001 --ipc=host
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          dest: ~/setup-pnpm-${{ github.job }}

      - name: Setup Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run E2E & smoke tests
        run: pnpm test:e2e:ci

      - name: Report retry flakes
        if: always()
        run: node scripts/report-retry-flakes.mjs
        env:
          # Used to file/append a deduped `retry-flake` issue per offending
          # test; absent/read-only (fork PRs) degrades to annotation-only —
          # see the GITHUB_TOKEN check in scripts/report-retry-flakes.mjs.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: playwright-report
          # test-results/ carries the on-first-retry trace zips + failure
          # screenshots (Playwright's outputDir default); playwright-report/
          # carries the JSON/HTML report. Neither dir is nested in the other.
          path: |
            playwright-report/
            test-results/
          retention-days: 14

  # ---------------------------------------------------------------------------
  # Job 5: Deploy preview and comment on PR
  # ---------------------------------------------------------------------------
  preview:
    name: Preview Deploy
    needs: [build-site, build-history]
    # Skip on fork PRs: CF secrets are not available to fork-originated workflows,
    # so the deploy step would fail with an empty token. Same-repo PRs always run.
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Download site build output (from build-site)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: site-dist
          path: ${{ runner.temp }}
      - name: Extract site build output
        run: mkdir -p dist && tar -xf "$RUNNER_TEMP/site-dist.tar" -C dist

      - name: Download doc history output (from build-history)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        # Tolerant: doc history is an enhancement, not required to deploy. Mirrors
        # the original restore that omitted fail-on-cache-miss — the Prepare step
        # guards on the dir existing before merging.
        continue-on-error: true
        with:
          name: doc-history
          path: ${{ runner.temp }}
      - name: Extract doc history output
        continue-on-error: true
        run: mkdir -p doc-history-out && tar -xf "$RUNNER_TEMP/doc-history.tar" -C doc-history-out

      - name: Prepare dist for deployment
        # Workers static assets: deploy directly from dist/ (no deploy/pj/zudo-doc/
        # nesting). zfb emits a flat dist/ — the dist/client/ Astro hybrid-mode
        # shape is never produced. Write .assetsignore so _worker.js and
        # _zfb_inner.mjs are not served as downloadable public files.
        run: |
          # (g) Merge doc history JSONs into dist/ directly.
          if [ -d doc-history-out ] && [ "$(ls -A doc-history-out 2>/dev/null)" ]; then
            mkdir -p dist/doc-history
            cp -r doc-history-out/. dist/doc-history/
          fi
          # Exclude worker bundle files from the public asset directory.
          printf '%s\n' '_worker.js' '_zfb_inner.mjs' > dist/.assetsignore

      - name: Deploy PR preview to Cloudflare Workers
        id: cf-deploy
        run: |
          set -o pipefail
          PR_NUMBER="${{ github.event.pull_request.number }}"
          ALIAS="pr-${PR_NUMBER}"
          # pr-${PR_NUMBER} is alias-safe (lowercase, no slashes/underscores).
          # `wrangler versions upload` has no --json flag; it prints the stable
          # preview-alias URL (on *.workers.dev — the generated preview config
          # enables both preview_urls and workers_dev) to stdout/stderr. Capture
          # both via tee and grep
          # the alias URL line, anchored on the .workers.dev boundary. Parse the
          # alias URL (subdomain starts with the alias) — not the per-version
          # hash URL — so smoke gates + PR comments hit a stable host.
          DEPLOY_LOG=$(mktemp)
          # Cloudflare does not generate preview URLs for versions implementing
          # Durable Objects. Upload the already-built adapter graph through an
          # adapter-only entry to a separate preview service; production alone
          # uses worker-entry.ts and applies the DO migration.
          PREVIEW_CONFIG=$(mktemp "$PWD/.wrangler-preview.XXXXXX.toml")
          trap 'rm -f "$PREVIEW_CONFIG"' EXIT
          node scripts/write-worker-preview-config.mjs "$PREVIEW_CONFIG"
          upload_preview() {
            : > "$DEPLOY_LOG"
            npx wrangler@4.85.0 versions upload \
              --config "$PREVIEW_CONFIG" \
              --preview-alias "$ALIAS" \
              2>&1 | tee "$DEPLOY_LOG"
          }
          upload_preview
          DEPLOY_URL=$(grep -oE "https://${ALIAS}-[a-z0-9.-]+\.workers\.dev" "$DEPLOY_LOG" | head -1 || true)
          if [ -z "$DEPLOY_URL" ]; then
            # A new service is created by the first upload, but versions upload
            # does not apply its non-versioned subdomain settings. Enable the
            # isolated service explicitly, then create the aliased version.
            curl --fail-with-body --silent --show-error \
              --request POST \
              --url "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/workers/scripts/zudo-doc-preview/subdomain" \
              --header "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
              --header "Content-Type: application/json" \
              --data '{"enabled":true,"previews_enabled":true}' \
              | jq -e '.success and .result.enabled and .result.previews_enabled' > /dev/null
            upload_preview
            DEPLOY_URL=$(grep -oE "https://${ALIAS}-[a-z0-9.-]+\.workers\.dev" "$DEPLOY_LOG" | head -1 || true)
          fi
          rm -f "$PREVIEW_CONFIG"
          trap - EXIT
          if [ -z "$DEPLOY_URL" ]; then
            echo "::error::Failed to parse alias preview URL for '$ALIAS' from wrangler output"
            exit 1
          fi
          echo "deploy_url=$DEPLOY_URL" >> "$GITHUB_OUTPUT"
          echo "Preview URL: $DEPLOY_URL"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Guardrail: assert that the deployed PR preview HTML has root asset URLs
      # (stylesheet href and islands script src). Under Workers static assets
      # with base="/", asset paths are root-relative (/assets/...) not
      # /pj/zudo-doc/-prefixed. If zfb silently drops the `base` threading in
      # a future version this step goes red before the broken preview reaches
      # the user — fixing the gap documented in epic #1386 / #1389.
      - name: Verify deployed PR preview has root asset URLs
        run: |
          set -euo pipefail
          PREVIEW_URL="${{ steps.cf-deploy.outputs.deploy_url }}/"
          echo "Checking root asset URLs at: $PREVIEW_URL"
          HTML=$(curl -fsSL "$PREVIEW_URL")
          # Use here-strings (not echo|grep) so grep -q exiting early on a
          # match cannot SIGPIPE upstream echo + flip pipefail.
          grep -qE "href=[\"']?/assets/" <<<"$HTML" \
            || { echo "::error::Preview HTML missing root stylesheet href"; exit 1; }
          grep -qE "src=[\"']?/assets/" <<<"$HTML" \
            || { echo "::error::Preview HTML missing root islands script src"; exit 1; }
          echo "OK: deployed PR preview has root asset URLs"

      # Public-asset HTTP 200 gate — validates zfb native publicDir (>= 0.1.0-next.62)
      # copies public/ → dist/ at base="/" so /img/logo.svg serves 200 (#2358,
      # replacing the old copy-public-plugin.mjs workaround from #1394).
      - name: Public-asset smoke gate (native publicDir)
        env:
          DEPLOY_URL: ${{ steps.cf-deploy.outputs.deploy_url }}
        run: |
          set -euo pipefail
          STATUS=$(curl -fsSL -o /dev/null -w '%{http_code}' "$DEPLOY_URL/img/logo.svg")
          [ "$STATUS" = "200" ] || { echo "::error::$DEPLOY_URL/img/logo.svg returned HTTP $STATUS, expected 200"; exit 1; }
          echo "OK: $DEPLOY_URL/img/logo.svg returned HTTP 200"

      # CSS-shape gate — defends against #1395 (Sub-4 host-side workaround:
      # @theme { --color-*: initial; --spacing: initial; } reset).
      # Three structural invariants live in the composite action so all three
      # deploy workflows stay in sync automatically.
      - name: CSS-shape smoke gate (#1395)
        uses: ./.github/actions/css-shape-smoke-gate
        with:
          deploy-url: ${{ steps.cf-deploy.outputs.deploy_url }}

      # AI-chat endpoint smoke gate — assert POST /api/ai-chat returns a JSON
      # content-type (not a static 404 page). A text/html response means the
      # Worker SSR route is not being reached (Workers static-assets placement
      # regression). HTTP 4xx from the endpoint is acceptable (empty body may
      # be rejected); only the content-type matters.
      - name: AI-chat endpoint smoke gate
        env:
          DEPLOY_URL: ${{ steps.cf-deploy.outputs.deploy_url }}
        run: |
          set -euo pipefail
          CONTENT_TYPE=$(curl -s -o /dev/null -w '%{content_type}' \
            -X POST "$DEPLOY_URL/api/ai-chat" \
            -H 'Content-Type: application/json' \
            -d '{}')
          echo "POST /api/ai-chat → Content-Type: $CONTENT_TYPE"
          echo "$CONTENT_TYPE" | grep -qi 'application/json' \
            || { echo "::error::POST /api/ai-chat returned Content-Type '$CONTENT_TYPE', expected application/json (static 404 fallback?)"; exit 1; }
          echo "OK: /api/ai-chat returns JSON content-type"

      - name: Comment preview URL on PR
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          DEPLOY_URL: ${{ steps.cf-deploy.outputs.deploy_url }}
        with:
          script: |
            const deployUrl = process.env.DEPLOY_URL;
            const prNumber = context.issue.number;
            const marker = '<!-- cf-preview-pr -->';

            const body = [
              marker,
              `## Cloudflare Workers Preview`,
              '',
              `Preview deployment is ready.`,
              '',
              `| | URL |`,
              `|---|---|`,
              `| Preview | ${deployUrl} |`,
              '',
              `Built from commit: \`${context.sha.substring(0, 7)}\``,
            ].join('\n');

            // Look for an existing comment from this workflow
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });

            const existing = comments.find(c => c.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
              console.log(`Updated existing preview comment (id: ${existing.id})`);
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body,
              });
              console.log('Created new preview comment');
            }
