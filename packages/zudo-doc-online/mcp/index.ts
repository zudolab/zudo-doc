// Stub — the MCP stdio server wrapping the REST API lands in sub-issue #3333.
// This placeholder only proves the `mcp` script wiring (tsx) works before
// that server exists.
console.log("zudo-doc-online MCP server: not implemented yet (see #3333)");
