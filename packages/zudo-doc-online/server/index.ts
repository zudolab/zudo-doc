// Stub — the transactional file-store + REST + SSE API server lands in
// sub-issue #3331. This placeholder only proves the `dev:server` script
// wiring (tsx watch) works before that server exists.
console.log("zudo-doc-online API server: not implemented yet (see #3331)");
