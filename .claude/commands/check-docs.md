---
description: Check documentation for broken links and formatting issues
---

Review all MDX files in `src/content/docs/` for:

1. Broken internal links
2. Missing frontmatter (`title` is required)
3. Invalid MDX syntax (unescaped `<`, `{`, `}`)
4. Missing `sidebar_position` on index pages

Report findings as a checklist.
