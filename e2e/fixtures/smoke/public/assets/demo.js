const answer = 42;
function greet(name) {
  return `Hello, ${name}!`;
}
const message = greet("asset viewer");
console.log(message); // A deliberately long line keeps the wrap control measurable in the viewer: asset-viewer smoke coverage needs horizontal overflow before Wrap can be toggled, so this comment intentionally continues across the code band.
