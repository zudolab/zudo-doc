# 3740-asset-viewer-proto

Design reference for the Asset Viewer epic (#3740, supersedes #3739). Standalone HTML/CSS/JS prototype — open `index.html` in a browser (no build step). Pages 01–07 cover the code viewer (A: document-style, B: sticky file bar), image (with the image-enlarge dialog), video, zip fallback, PDF, and the doc-side entry points.

Throwaway: never import it into product code. Delete this directory before the root PR merges (S12).
