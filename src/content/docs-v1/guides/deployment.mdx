---
title: Deployment
description: Deploying zudo-doc v1.0
sidebar_position: 2
---

## Static Build

Run the Astro build command to produce a static `dist/` folder:

```bash
pnpm build
```

## Hosting

Deploy the `dist/` folder to any static host (Netlify, Vercel, GitHub Pages, or a plain CDN bucket).
